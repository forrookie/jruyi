Usage: scr:disable <componentId>

Disable the given component if not already disabled. If the component is
already destroyed or disabled, this command has no effect.

'componentId' may be either the ID of a component or the component's name.
